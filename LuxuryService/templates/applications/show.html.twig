{% extends 'base.html.twig' %}

{% block title %}Applications{% endblock %}

{% block body %}
    <h1>Applications</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ application.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_applications_index') }}">back to list</a>

    <a href="{{ path('app_applications_edit', {'id': application.id}) }}">edit</a>

    {{ include('applications/_delete_form.html.twig') }}
{% endblock %}
